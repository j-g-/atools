import { ProductSaleDetail } from './product-sale-detail';

describe('ProductSaleDetail', () => {
  it('should create an instance', () => {
    expect(new ProductSaleDetail()).toBeTruthy();
  });
});
